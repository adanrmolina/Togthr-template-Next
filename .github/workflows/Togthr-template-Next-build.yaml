# To better understand this file read this:
# https://docs.ionos.space/docs/github-actions-customization/

name: "Deploy Now: Execute Build"

on:
  workflow_call:
    inputs:
      site-url:
        required: true
        type: string
      branch-id:
        required: true
        type: string

env:
  DEPLOYMENT_FOLDER: out

jobs:
  # Please do not rename this job
  build-project:
    name: build Togthr-template-Next
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v4
        with:
          submodules: 'recursive'

      - name: Setup Node (use 18 for Next 12)
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install deps from lockfile
        env:
          NODE_ENV: production
        run: npm ci

      - name: Build & Export static site
        env:
          CI: true
          SITE_URL: ${{ inputs.site-url }}
          NEXT_TELEMETRY_DISABLED: 1
        run: |
          npm run build
          npm run export

      # Please do not touch the following action
      - name: Store deployment content
        uses: ionos-deploy-now/artifact-action@v1
        with:
          api-key: ${{ secrets.IONOS_API_KEY }}
          service-host: api-us.ionos.space
          project-id: 89d51bd0-2e9c-41b7-b495-60c58532221c
          branch-id: ${{ inputs.branch-id }}
          version: ${{ github.sha }}
          folder: ${{ env.DEPLOYMENT_FOLDER }}
          config-file: .deploy-now/Togthr-template-Next/config.yaml
          action: upload
